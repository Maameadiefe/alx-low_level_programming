#include "main.h"

/**
 * factorial - This untion returns the factorialof a given number
 * @x: the given number
 * Return: Value
 */
int factorial(int n)
{
	if (x <= 1)
		return (1);
	else
		return (x * factorial(x - 1));
}
