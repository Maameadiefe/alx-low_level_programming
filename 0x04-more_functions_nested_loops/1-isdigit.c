#include "main.h"

/**
 * _isdigit - Chests if a charater is a digit
 * @y : The charater to check
 * Return: 1 if the character is a digit else 0
 */

int _isdigit(int y)

{
	if (y >= 48 && y <= 57)
	{
	return (1);
	}
	return (0);
}
